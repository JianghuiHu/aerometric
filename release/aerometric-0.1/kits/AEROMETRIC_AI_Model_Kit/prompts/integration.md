# Integrate an AEROMETRIC model

Read `AI_START_HERE.md`. Load the GLB transactionally: parse and validate a temporary model session before replacing the current model. Preserve source transforms and dimensions, center only a preview wrapper, and fit the camera. Drive UI availability from validated capabilities. Route mesh and material access through an adapter. Export the evaluated model root and verify the result by re-importing it.

