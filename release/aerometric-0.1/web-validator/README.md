# AEROMETRIC Web Validator 0.1

Client-side GLB and optional Profile validation. Files are parsed locally and are not uploaded. The Web and CLI validators import the same pure rule module at `validator/src/rules.mjs`.

From the Studio repository root:

```text
npm run build:web-validator
npm run preview:web-validator
```
